# Findings

Where out-of-spec issues go the moment they are found, instead of being fixed on
the spot or forgotten. Anything discovered while a work unit is in flight that is
not required by that unit lands here as one row: what, where, the evidence it is
real, and a suspected severity.

**Nothing here is scheduled work.** The owner reviews each row and approves it
individually. An unreviewed row is a report, not a commitment — which is what
keeps "I noticed something" from turning into unplanned scope.

Issues that make the *current* work unit's own claim false are in spec by
definition and get fixed immediately, whatever file they turn up in. The test is
the claim, not the file.

The rule that decides which side of the line something falls on is in
[triage.md](triage.md).

---

## Open

| # | Finding | Where | Evidence | Severity | Reviewed |
| --- | --- | --- | --- | --- | --- |
| F1 | The base set is argued in two places and could drift | [README.md](../../README.md) "Why three modules are base", and [SPEC.md](../../SPEC.md) "Base" | Both carry the what-breaks-without-it justification for the three base modules. The wording already differs — the README says "cannot be wrong about anything" where SPEC.md says "unfalsifiable". Neither is wrong today; nothing keeps them together tomorrow, and no check can see prose agreeing with prose. | Low. Both statements are currently true, and the base set is fixed at three by a rule that requires a major version to change. The risk is a future edit to one that is not made to the other. | No |
| F2 | `role_referenced` cannot see a reference written as prose | [SPEC.md](../../SPEC.md) check kinds | The check that catches a record pointing at a destination that has moved resolves markdown links only. The real drift it was modelled on — a triage document naming its findings destination in a sentence rather than a link — would not be caught if the destination were named in prose instead of linked. `TR-03`'s fixture uses a link, so the fixture passes while the general case leaks. | Moderate for what the check claims. It is the highest-value check in the catalog and its coverage is narrower than its obligation reads. Options are narrowing the obligation's wording or adding a kind that matches a path-shaped string anywhere in the target; the second produces false positives on any prose that quotes a path. | No |
| F3 | Found in M8. `triage-rule`'s `expected.md` states a judgment-line count that matches no reading of its own fixtures | [modules/triage-rule/fixtures/expected.md](../../modules/triage-rule/fixtures/expected.md) `satisfies/` | The line reads "Four `judgment` lines still emitted". [modules/triage-rule/module.yaml](../../modules/triage-rule/module.yaml) declares two judgment entries, `triage.boundary-is-a-claim` and `triage.destination-is-real`, and `git log` shows it has declared exactly two since the module was written in M3. The tree also adopts `findings-queue`, whose four entries would make six for the whole run — but the same file scopes itself to `TR-*` results only. Neither two nor six is four. `expected.yaml` lists the two ids the manifest declares, so prose and data now disagree in the open. | Low for correctness — no check result changes, and `expected.yaml` now carries a count a run can be compared against. Moderate for the prose file's authority: an implementer who trusts the number concludes their run emitted too few judgment lines and goes looking for a defect in their own checker. | No |
| F4 | Found in M10. `SPEC.md` does not say what state a check reports while its two halves disagree | [SPEC.md](../../SPEC.md) "What a module is", the *prose is normative* paragraph | The paragraph says `module.md` wins and "the disagreement is a defect — report it, do not pick", and a check must report one of exactly five states. It names no state for this case. Found concretely by the first checker on `WU-04`, whose halves disagreed: the implementation had to choose between reporting `ok` (following the normative half literally, failing `violates-WU-04`) and reporting `finding` (following the manifest, doing the thing the paragraph calls hiding a bug). Both are reachable from what is written. The wording proposed to close it is in [A06](https://github.com/DevOfPie/strucgu-check/blob/main/ambiguities/A06-wu-04-pattern-disagrees-between-halves.md); it was deliberately not taken in M10 because it would change `WU-04`'s expected result from `finding` to `skip`, which is an expectation change rather than a wording fix. | Moderate. `WU-04`'s own instance is repaired, so nothing is currently ambiguous — but the next disagreement between a module's two halves lands in the same hole, and the specification's answer will still be "report it" with nowhere to report it from. | No |
| F5 | Found in M10. `expected.yaml` cannot express a tree whose expectation is that the audit refuses to start | [SPEC.md](../../SPEC.md) "Fixture expectations", and every `modules/*/fixtures/expected.yaml` | `docs/auditing.md` defines a closed list of conditions under which a run cannot start — no adoption record, an unparseable one, an inexact version pin, a missing module directory, a `forms` module with no `form`, and now a `history_deletions` check with no `effective_from`. None of those conditions can be pinned by a fixture: `expected.yaml` has two sections and a value vocabulary closed at the five audit states, and none of the five means *the audit could not start*. So six stated behaviours have no tree behind them and two implementations may differ on all six while reproducing every fixture. Observed while writing the patch for [A08](https://github.com/DevOfPie/strucgu-check/blob/main/ambiguities/A08-pinned-version-does-not-match-the-module.md), which wanted such a tree and could not write one. | Moderate. The same class as `F3` — untested prose — but wider, and it covers the conditions under which a checker should refuse rather than report, which is where a wrong implementation does the most damage silently. Closing it needs either a third section in `expected.yaml` or a per-tree expectation outside the five states. | No |
| F6 | Found in M10. `DL-03` reports a finding against this repository | [docs/records/decisions.md](decisions.md), history after `effective_from: 2026-07-31` | The first checker run over this repository reports: `finding DL-03 docs/records/decisions.md — e132646 "Correct the plan's decisions by appending, and redact by removing" removed 4 line(s) (and 1 more), after 2026-07-31`. The commit is real and its message says what it did. `decision-log`'s own [module.md](../../modules/decision-log/module.md) documents `DL-03` as reported "for a look, not as a defect" and lists many false positives by construction, and the entry [Enumerating a private repository's record shapes was itself the leak](decisions.md#enumerating-a-private-repositorys-record-shapes-was-itself-the-leak) argues the redaction. The check cannot tell a justified redaction from an entry edited away, which is the check working as specified. | Low as a defect, moderate as a decision. Nothing is wrong with the log. What is missing is the disposition: either this repository records its first deviation — the mechanism is specified in three documents and [documented as untested by use](../../CHANGELOG.md) — or the finding is accepted every run forever, which is what a queue of unreviewed findings looks like from the outside. | No |
| F7 | Found in M10. Nothing says what a MAJOR bump means while the catalog is below `1.0.0` | [SPEC.md](../../SPEC.md) "Versioning", against [README.md](../../README.md) "What 0.x means here" | `SPEC.md` names three bump levels by what an adopter newly sees, and the inherited rules in [work/README.md](work/README.md) make a new check a major version in absolute terms. Every version in the repository is `0.x`, where the first digit conventionally has not started moving. No document says whether a major means `1.0.0` or collapses into the minor digit, and the two answers are not equivalent: `1.0.0` would assert a stability `README.md` explicitly disclaims — "the shape will move, pins are cheap, breaking changes will happen". Surfaced by `work-units`' bump, which was decided as a patch precisely so that this question did not have to be answered off an instance that did not need it. | Low today, because nothing has needed a major and three of the things that would need one — a new module, growth of the base set, a new check — are all out of scope for this phase. It becomes blocking the first time one is wanted, and the wrong moment to settle a versioning convention is while shipping the change that forces it. | No |

## Closed

*(empty)*
