# Findings

Where out-of-spec issues go the moment they are found, instead of being fixed on
the spot or forgotten. Anything discovered while a work unit is in flight that is
not required by that unit lands here as one row: what, where, the evidence it is
real, and a suspected severity.

**Nothing here is scheduled work.** The owner reviews each row and approves it
individually. An unreviewed row is a report, not a commitment — which is what
keeps "I noticed something" from turning into unplanned scope.

Issues that make the *current* work unit's own claim false are in spec by
definition and get fixed immediately, whatever file they turn up in. The test is
the claim, not the file.

The rule that decides which side of the line something falls on is in
[triage.md](triage.md).

---

## Open

| # | Finding | Where | Evidence | Severity | Reviewed |
| --- | --- | --- | --- | --- | --- |
| F3 | Found in M8. `triage-rule`'s `expected.md` states a judgment-line count that matches no reading of its own fixtures | [modules/triage-rule/fixtures/expected.md](../../modules/triage-rule/fixtures/expected.md) `satisfies/` | The line reads "Four `judgment` lines still emitted". [modules/triage-rule/module.yaml](../../modules/triage-rule/module.yaml) declares two judgment entries, `triage.boundary-is-a-claim` and `triage.destination-is-real`, and `git log` shows it has declared exactly two since the module was written in M3. The tree also adopts `findings-queue`, whose four entries would make six for the whole run — but the same file scopes itself to `TR-*` results only. Neither two nor six is four. `expected.yaml` lists the two ids the manifest declares, so prose and data now disagree in the open. | Low for correctness — no check result changes, and `expected.yaml` now carries a count a run can be compared against. Moderate for the prose file's authority: an implementer who trusts the number concludes their run emitted too few judgment lines and goes looking for a defect in their own checker. | No |
| F4 | Found in M10. `SPEC.md` does not say what state a check reports while its two halves disagree | [SPEC.md](../../SPEC.md) "What a module is", the *prose is normative* paragraph | The paragraph says `module.md` wins and "the disagreement is a defect — report it, do not pick", and a check must report one of exactly five states. It names no state for this case. Found concretely by the first checker on `WU-04`, whose halves disagreed: the implementation had to choose between reporting `ok` (following the normative half literally, failing `violates-WU-04`) and reporting `finding` (following the manifest, doing the thing the paragraph calls hiding a bug). Both are reachable from what is written. The wording proposed to close it is in [A06](https://github.com/DevOfPie/strucgu-check/blob/main/ambiguities/A06-wu-04-pattern-disagrees-between-halves.md); it was deliberately not taken in M10 because it would change `WU-04`'s expected result from `finding` to `skip`, which is an expectation change rather than a wording fix. | Moderate. `WU-04`'s own instance is repaired, so nothing is currently ambiguous — but the next disagreement between a module's two halves lands in the same hole, and the specification's answer will still be "report it" with nowhere to report it from. | No |
| F5 | Found in M10. `expected.yaml` cannot express a tree whose expectation is that the audit refuses to start | [SPEC.md](../../SPEC.md) "Fixture expectations", and every `modules/*/fixtures/expected.yaml` | `docs/auditing.md` defines a closed list of conditions under which a run cannot start — no adoption record, an unparseable one, an inexact version pin, a missing module directory, a `forms` module with no `form`, and now a `history_deletions` check with no `effective_from`. None of those conditions can be pinned by a fixture: `expected.yaml` has two sections and a value vocabulary closed at the five audit states, and none of the five means *the audit could not start*. So six stated behaviours have no tree behind them and two implementations may differ on all six while reproducing every fixture. Observed while writing the patch for [A08](https://github.com/DevOfPie/strucgu-check/blob/main/ambiguities/A08-pinned-version-does-not-match-the-module.md), which wanted such a tree and could not write one. | Moderate. The same class as `F3` — untested prose — but wider, and it covers the conditions under which a checker should refuse rather than report, which is where a wrong implementation does the most damage silently. Closing it needs either a third section in `expected.yaml` or a per-tree expectation outside the five states. | No |
| F6 | Found in M10. `DL-03` reports a finding against this repository | [docs/records/decisions.md](decisions.md), history after `effective_from: 2026-07-31` | The first checker run over this repository reports: `finding DL-03 docs/records/decisions.md — e132646 "Correct the plan's decisions by appending, and redact by removing" removed 4 line(s) (and 1 more), after 2026-07-31`. The commit is real and its message says what it did. `decision-log`'s own [module.md](../../modules/decision-log/module.md) documents `DL-03` as reported "for a look, not as a defect" and lists many false positives by construction, and the entry [Enumerating a private repository's record shapes was itself the leak](decisions.md#enumerating-a-private-repositorys-record-shapes-was-itself-the-leak) argues the redaction. The check cannot tell a justified redaction from an entry edited away, which is the check working as specified. | Low as a defect, moderate as a decision. Nothing is wrong with the log. What is missing is the disposition: either this repository records its first deviation — the mechanism is specified in three documents and [documented as untested by use](../../CHANGELOG.md) — or the finding is accepted every run forever, which is what a queue of unreviewed findings looks like from the outside. | No |
| F8 | Found in M11. A `judgment` line reports a role as unreadable when it is mapped and readable, and no fixture pins the case | [SPEC.md](../../SPEC.md) "Judgment entries", and [modules/triage-rule/module.yaml](../../modules/triage-rule/module.yaml) `triage.destination-is-real` | That entry declares `read: [triage_doc, findings]`. `findings` belongs to `findings-queue`, which cross-module binding makes legitimate — `TR-03` binds the same pair. Run against a tree that adopts both modules and maps both roles, [strucgu-check](https://github.com/DevOfPie/strucgu-check) still annotates the line `[unresolved role: findings — nothing to read]`. Reproduced against this repository's own tree, where `findings` is mapped and `FQ-01` reports `ok` in the same run. `SPEC.md` says the annotation appears "where a `read:` role is unmapped or unreadable"; it is neither. No fixture can catch it: `expected.yaml`'s `judgment` section carries one state per entry id and cannot express the annotation, so every implementation reproduces every fixture whether or not it gets this right. | Moderate. The failure is silent and points a person away from a document that is there — the opposite of the harm the annotation was added to prevent. It also lands in the one channel that exists to stop a mechanical run looking complete. Same family as `F5`: prose with no tree behind it. Fixing it needs either an expectation channel for the annotation or a rule that a judgment entry may not name a role outside its module, and the second would break `triage.destination-is-real` as written. | No |
| F9 | Found in M11. **Catalog gap — a module this catalog is missing.** No role names the document stating what is true and what is in scope for the project as a whole | [modules/](../../modules/), across all five | The second adopter's audit reached its process document (`triage_doc`), its rationale (`decision_log`) and its deferral destination (`findings`), and could not reach its scope contract at all. The only role that comes close is `work-units`' `unit_index`, described as "the index, and the one place rules common to all units are stated" — which binds the scope contract to a repository having units of work. That adopter has none and declined the module, so the most load-bearing record it keeps is invisible to every check in the catalog. This repository hides the gap from itself: [work/README.md](work/README.md) is both its scope contract and its unit index, so the coupling has never cost it anything. | Moderate. It is the largest uncovered record in a catalog about records of work, and the coupling means the coverage a repository gets depends on whether an unrelated optional module applies to it. Closing it is either a sixth module or splitting `unit_index` in two, and both are major-version arguments — see [work/README.md](work/README.md) "Not in this phase". | No |
| F10 | Found in M11. **Catalog gap — a module this catalog is missing.** No obligation anywhere gives a record an expiry that forces someone to dispose of it | [SPEC.md](../../SPEC.md) "The adoption record", and [modules/findings-queue/module.md](../../modules/findings-queue/module.md) `findings.queue-is-reviewed` | The only expiry in the whole catalog is `review_by` on a deviation, and it lives in the adoption record rather than in any module. Nothing obliges a queue row, a work unit, or any other record to carry a date past which a person must extend it, close it, or act. `findings-queue`'s own judgment entry names the unreviewed queue as "the module's failure mode" and says it "looks identical to a healthy one from the outside" — which is an admission that the module cannot see its own worst outcome. The second adopter carries a forcing date on every record of its kind and a stated rule that letting one lapse silently is not permitted; the audit could observe neither, because no role and no obligation asks. This repository has seven open rows in this file, none reviewed, and nothing in the catalog can tell. | Moderate to high for what the catalog claims. Every module here checks that a record exists and has a usable shape; none checks that anyone ever comes back to it, and a record nobody returns to is the failure every one of these modules was extracted to prevent. Closing it is an obligation on `findings-queue` at minimum and arguably a module of its own, and either is a major version. | No |
| F11 | Found in M11. **Catalog gap — out of scope for a catalog about records of work.** No role names a record kept solely so that a later search finds a prior rejection | [modules/decision-log/module.md](../../modules/decision-log/module.md), against [SPEC.md](../../SPEC.md) "Structure and use, not content" | The second adopter keeps a record of everything it has rejected, whose entire purpose is that the next person proposing the same thing finds the reason it died instead of filing it as new. `decision-log` records why a choice was made, but it is navigated by index and date; nothing obliges an entry to be findable by the subject it rejected. The answer is nonetheless **out of scope**, and the reason is the catalog's keystone rule: an obligation that a record be discoverable by its subject is an obligation about what the record says, not about its shape or its use. Any check for it would either measure the presence of a word or be satisfiable by writing one, which is the cut criterion in [triage.md](triage.md). | Low, and recorded because the reasoning is the useful part. The gap is real, and it stays uncovered on principle rather than by omission — this is the shape of thing that would arrive as a module request and should be declined with an argument rather than silence. | No |
| F12 | Found in M11. **Catalog gap — out of scope for a catalog about records of work.** No role names a record whose filled form leaves this repository to become another repository's contract | [modules/work-units/module.md](../../modules/work-units/module.md), the `unit_template` role | `unit_template` is "the skeleton new records start from" — a template consumed inside the repository that holds it. The second adopter keeps a template whose filled instance is lifted out verbatim to become a different repository's scope contract on the day that repository is created, which is a handoff artifact rather than a skeleton. Out of scope: once lifted, the artifact is the receiving repository's record and the catalog's existing roles cover it there. A role for the sending side would oblige a repository to declare something about a repository that does not exist yet, and nothing here can check a claim about a tree it cannot read — the same reason `history_deletions` reads only the audited root. | Low. Nothing is uncovered that matters once the handoff happens, and the gap is a consequence of a boundary the specification draws deliberately. | No |
| F13 | Found in M11. A judgment entry reads for a field no obligation in its module requires | [modules/findings-queue/module.md](../../modules/findings-queue/module.md) `findings.severity-argues` | The entry is declared under `obligation: findings-carry-evidence`, whose purpose is that a row records what was observed. Severity is a different field and no obligation in the module mentions it — [module.yaml](../../modules/findings-queue/module.yaml)'s four obligations are destination, evidence, review state and unscheduled. Surfaced when the second adopter's queue was audited: the entry asks a person to grade a field that repository has no reason to keep and that the module never asked for. This repository's queue happens to have a `Severity` column, which is why the entry has read as sensible for two releases. | Low. It costs a person one wasted read per run and, more quietly, makes the judgment channel look like it covers ground the obligations do not. Fix is either an obligation that a row argues its impact — which would be a new obligation and a major version — or rewording the entry to read against evidence, which is a patch. | No |
| F14 | Found in M11. Five fixture expectation rows are no longer reproduced by the only implementation, and nothing here notices | [modules/decision-log/fixtures/expected.yaml](../../modules/decision-log/fixtures/expected.yaml), `DL-03` in `satisfies` and four `violates-*` trees | Running [strucgu-check](https://github.com/DevOfPie/strucgu-check)'s conformance harness over this repository's fixtures at `0.3.0` gives `36 trees, 337 expectation rows, 5 mismatch(es)`, all of the form `DL-03: expected skip, got ok`. The expectations are right: `decision-log` `0.3.0` changed `DL-03` to report `skip` where no history is readable, [CHANGELOG.md](../../CHANGELOG.md) says so, and a fixture tree is not a git repository. The implementation is behind. Pre-existing and not caused by M11 — no fixture or module was touched by it — and observed only because M11 ran the harness as a gate. | Moderate, and not about the mismatch. Propagation here is pull-only by design, so an implementation falling behind is expected. What is missing is that nothing in this repository records which implementations reproduce which release, so the one piece of evidence a prospective adopter would want — *has anyone implemented this, and does their run still match?* — has no home. `M9`'s conformance document is the natural place and it is deferred. **M9 has since been written and declined to take it** — [conformance.md](../conformance.md) states that no such record is kept, and [why](decisions.md#the-register-a-prospective-adopter-wants-is-the-register-a-standards-body-keeps). The row stays open: the five mismatches are unchanged, and what it asks for is now a stated refusal carrying its cost rather than an empty place. | No |
| F15 | Found in M12. `docs/auditing.md` describes a module's `fixtures/` directory without `expected.yaml`, which has been a required file since M8 | [docs/auditing.md](../auditing.md) "Verifying your implementation" | The listing names `satisfies/`, the `violates-*` trees and `expected.md`, and the sentence after it says a run is verified against "the finding named in `expected.md`". [SPEC.md](../../SPEC.md) "What a module is" requires `expected.yaml` in every module directory, and [SPEC.md](../../SPEC.md#fixture-expectations) says it exists so that *did my run match?* is answered mechanically rather than by reading prose. The one document written for the person implementing a checker sends them to the prose file. Noticed while editing that listing to admit a third tree shape; the omission predates M12 and was not caused by it. | Low. Nothing is wrong that is stated wrongly — the file is incomplete rather than untrue, and an implementer who reads `SPEC.md` finds the file. Moderate for the audience it misses: the harness that answers the only question an implementer has is invisible in the guide addressed to them. | No |
| F18 | Found in M13. This repository's own adoption record is pinned a release behind every module it adopts, and the review that would move it has now happened | [strucgu.yaml](../../strucgu.yaml), against `modules/*/module.yaml` | All five modules are pinned `version: "0.1.0"`. They ship at `triage-rule` 0.4.0, `decision-log` 0.3.0, `findings-queue` 0.3.0, `investigations` 0.2.1, `work-units` 0.2.1, so every module section of the [0.2.0 self-walk](self-walk-0.2.0.md) emits a drift notice. Leaving fixture-tree pins behind is [deliberate and argued](decisions.md#the-modules-bump-to-020-and-no-adoption-record-moves-with-them) — a pin is a dated claim about what was reviewed — but that argument names the fixture trees, and this file is not one. A pin records what an adopter reviewed; the release walk *is* that review, performed against every module at its shipped version, so this repository now holds the review its own pin says it has not done. | Low mechanically — [SPEC.md](../../SPEC.md#versioning) is explicit that a checker evaluates the module it reads, so nothing runs differently and the notice is the mechanism working. Moderate as a claim: the one adoption record this repository controls says it agreed to five modules it has not agreed to since July, while the file's own comments describe mappings decided in `M10`. Fixing it is five lines and a dated note, and it touches a manifest, which [triage.md](triage.md) classes as work. | No |
| F20 | Found while revalidating the phase-three plan. **A link that resolves and misdirects passes every gate here.** The commit gate checks that relative links and anchors resolve; nothing checks that the prose around a link still describes what it points at | [triage.md](triage.md) *Before completing a commit*, the `Links` row; and [SPEC.md](../../SPEC.md) `links_resolve`, for the half of this that is the catalog's | Two entries in [decisions.md](decisions.md) named `M20` as the release unit and `M21` as the unit performing an adoption. Both units had since exchanged numbers, and no adoption unit exists in the phase at all. Every link resolved, so the gate passed, the link checker reported 0 broken across 60 files, and the defect was found by a person re-reading the plan against itself. Recorded in [decisions.md](decisions.md#m20-and-m21-swapped-roles-and-two-entries-above-still-point-the-old-way); the plan's scope contract [work/README.md](work/README.md) was correct throughout, so only the rationale was wrong | Low mechanically, and the severity is in the shape rather than the instance. **The catalog half is out of scope on principle** — whether prose describes its link's target correctly is content, not structure, and a check for it would be measuring what a record says rather than what shape it has, which is the cut criterion in [triage.md](triage.md) and the same reasoning that closed `F11` and `F12` out of scope. What is left is this repository's own gate reading stronger than it is: *every relative link and anchor resolves* is exactly what it does, and a reader takes it as coverage of whether the links are right. Same family as `F17` — the mechanism works as specified and its blind spot is invisible in every output it produces | No |

## Closed

Rows are moved here, never deleted. A queue that empties by deletion cannot show
what it caught, and the disposition is the part worth keeping.

| # | Finding | Where | Evidence | Severity | Reviewed | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | The base set is argued in two places and could drift | [README.md](../../README.md) "Why three modules are base", and [SPEC.md](../../SPEC.md) "Base" | Both carry the what-breaks-without-it justification for the three base modules. The wording already differs — the README says "cannot be wrong about anything" where SPEC.md says "unfalsifiable". Neither is wrong today; nothing keeps them together tomorrow, and no check can see prose agreeing with prose. | Low. Both statements are currently true, and the base set is fixed at three by a rule that requires a major version to change. The risk is a future edit to one that is not made to the other. | Yes, 2026-08-04 | **Closed in M12.** The argument now has one normative home, [SPEC.md](../../SPEC.md#base-and-prerequisites), and [README.md](../../README.md) links to it instead of restating it. SPEC.md was chosen because the base list is a contract property — `base: true` is a manifest field and growing the list is a versioning rule, both defined there — and because a README is edited for tone by people who are not editing the contract. See [decisions.md](decisions.md#the-base-set-argument-is-normative-in-specmd-and-the-readme-links-to-it). |
| F2 | `role_referenced` cannot see a reference written as prose | [SPEC.md](../../SPEC.md) check kinds | The check that catches a record pointing at a destination that has moved resolves markdown links only. The real drift it was modelled on — a triage document naming its findings destination in a sentence rather than a link — would not be caught if the destination were named in prose instead of linked. `TR-03`'s fixture uses a link, so the fixture passes while the general case leaks. | Moderate for what the check claims. It is the highest-value check in the catalog and its coverage is narrower than its obligation reads. Options are narrowing the obligation's wording or adding a kind that matches a path-shaped string anywhere in the target; the second produces false positives on any prose that quotes a path. | Yes, 2026-08-04 | **Closed in M12, by narrowing the obligation rather than widening the check.** `triage-destination` now reads *the document links to where out-of-scope findings go*; `TR-03` is unchanged; `modules/triage-rule/fixtures/satisfies-prose-destination/` pins that a rule naming its destination in prose is `ok` on the strength of a link elsewhere in the document. The rejected option — a check kind matching a path-shaped string anywhere — is named with its cost in [decisions.md](decisions.md#the-obligation-is-narrowed-to-what-the-check-can-see-rather-than-the-check-widened-to-prose). triage-rule 0.4.0. |
| F7 | Found in M10. Nothing says what a MAJOR bump means while the catalog is below `1.0.0` | [SPEC.md](../../SPEC.md) "Versioning", against [README.md](../../README.md) "What 0.x means here" | `SPEC.md` names three bump levels by what an adopter newly sees, and the inherited rules in [work/README.md](work/README.md) make a new check a major version in absolute terms. Every version in the repository is `0.x`, where the first digit conventionally has not started moving. No document says whether a major means `1.0.0` or collapses into the minor digit, and the two answers are not equivalent: `1.0.0` would assert a stability `README.md` explicitly disclaims — "the shape will move, pins are cheap, breaking changes will happen". Surfaced by `work-units`' bump, which was decided as a patch precisely so that this question did not have to be answered off an instance that did not need it. | Low today, because nothing has needed a major and three of the things that would need one — a new module, growth of the base set, a new check — are all out of scope for this phase. It becomes blocking the first time one is wanted, and the wrong moment to settle a versioning convention is while shipping the change that forces it. | Yes, 2026-08-04 | **Closed in M13, by the owner, on the instance the row predicted.** Below `1.0.0` a breaking change collapses into the minor digit: this release is breaking and is `0.2.0` rather than `1.0.0`, because `1.0.0` would assert the stability [README.md](../../README.md) disclaims. It arrived exactly as the row said it would — while shipping the change that forces it — which is why the answer is the owner's and is recorded as his rather than derived inside the work unit. See [decisions.md](decisions.md#below-100-a-breaking-change-collapses-into-the-minor-digit), and the conflict between the two versioning rules that produced the question, reported in [the entry above it](decisions.md#the-two-versioning-rules-measure-different-readers-and-the-conflict-is-reported-rather-than-picked). |
| F16 | Found in M9. The versioning table has no bump for a change that moves what a conformant run must output while an adopter sees nothing | [SPEC.md](../../SPEC.md#versioning), against [modules/decision-log/CHANGELOG.md](../../modules/decision-log/CHANGELOG.md) `0.3.0` | Bumps are defined by "what a previously clean adopter will newly see". `decision-log` `0.3.0` corrected five `DL-03` fixture rows from `ok` to `skip` and changed no check, no obligation and no role; its changelog states the required sentence as "nothing", and that is true — an adopter whose repository is a git repository sees no difference. The same release invalidated five rows of the only conformance claim in existence (`F14`). So the instrument the table asks every release to publish reports "nothing changed" about a release that moved a conformance boundary, and an implementer reading version numbers alone cannot tell which releases they must re-run against. MINOR was the right call under the table as written; the table has no row that fits. | Low today: the version did move, so a claim naming a version is still unambiguous, and [conformance.md](../conformance.md) needs no more than that. Moderate as the catalog acquires implementations — the fix is either a fourth row sized by what an *implementer* newly has to do, or an explicit statement that fixture corrections are MINOR and implementers re-run on every bump. Both are contract changes and neither is in [work/README.md](work/README.md)'s list of permitted specification edits this phase. | Yes, 2026-08-04 | **Closed in M15, and by neither option this row offered.** The versioning table does not change. [SPEC.md](../../SPEC.md#versioning) now requires a **second fixed sentence** in every module `CHANGELOG.md` entry, naming what an implementer must re-run, and states why one version number cannot carry both readers: a version string has three digits and the table has three rows for that reason, so a fourth level would either share a digit that already means something else or extend every version in the catalog with a field that exists for one audience. Both options the row named were declined with reasons — the fourth table row on that arithmetic, and *implementers re-run on every bump* because it makes a typo's PATCH indistinguishable from a release that moves a conformance boundary, which is the distinction this row was asking for. Entries published before 2026-08-04 are **not backfilled**, because an implementer consequence written after the fact is a reconstruction rather than a record; each module gains the sentence at its next release, which for all five is [M17](work/m17.md). [conformance.md](../conformance.md)'s clause 5 is reconciled rather than moved — it was true and stays true, and what changed is that an implementer reads the consequence from a sentence instead of inferring it from a digit that never carried it. The outcome is three statements measuring three readers and [SPEC.md](../../SPEC.md#versioning) says so plainly rather than presenting it as a reconciliation. The answer is the owner's and was [written down before it was acted on](decisions.md#the-implementers-signal-goes-in-the-changelog-not-in-the-version-number). |
| F17 | Found in M13. This file's index is one row short of its entries, and `DL-05` cannot see it | [decisions.md](decisions.md#index), against the entry *The orchestrator that landed M8 is disqualified from implementing M10* | The Index table is exactly one row short of the file's `###` entries — 51 against 52 when the release walk found it, 55 against 56 as shipped. The unindexed one is [The orchestrator that landed M8 is disqualified from implementing M10](decisions.md#the-orchestrator-that-landed-m8-is-disqualified-from-implementing-m10), appended on 2026-08-03 without its row, against that file's own instruction — "Append a row when you append an entry". `DL-05` reports `ok` regardless, because under `single-log` both roles resolve to one path and the check cannot fail while `DL-01` passes; this is the documented weakness the judgment entry `decision-log.index-exists-in-substance` exists to cover, and the [0.2.0 self-walk](self-walk-0.2.0.md#judgment) is where the gap was caught, on the second time of asking. Fixing it is one row and is not editing an entry. | Low for a reader who searches rather than browses. Moderate for what it demonstrates: the check that guards the index is structurally blind here, the judgment channel is the only thing that can see the gap, and it took two walks and a second repository's audit for anyone to look. An index that is 98% complete is the failure mode an index has — it reads as complete. | Yes, 2026-08-04 | **Closed in M14.** The row is added in file order, between *The first checker is DevOfPie/strucgu-check, and it is public* and *The fixtures were wrong about `DL-03`*. The index carried **70 rows against 71 `###` entries** before the fix and **71 against 71** immediately after. M14 then appended its own entry, with its own row, so the file ships at **72 against 72**; the counts are stated here rather than only in the commit message so that a reader who doubts the fix can count both patterns and get the same answer, and so that the second move is visible instead of looking like the first was mis-stated. No entry was edited — appending a row is what [decisions.md](decisions.md#index) asks for in its own instruction. `DL-05` reported `ok` before and after, and that does not change: the check is structurally blind here under `single-log`, the judgment entry `decision-log.index-exists-in-substance` is the only thing that could see the gap, and closing this row leaves that weakness exactly where it was documented. |
| F19 | Found while adding the `reopened` status word. [work/README.md](work/README.md) opens by stating that M8–M13 are scoped and unstarted, and that `planned` and `deferred` have not appeared in its table — both false since the phase closed | [work/README.md](work/README.md), the paragraph immediately under the status table | Every row M1–M13 in that same table reads `done`, and [CHANGELOG.md](../../CHANGELOG.md) records the release that closed the phase. The paragraph also defines `planned` and `deferred` as words the table has not used before; the table now uses neither, so two definitions survive with nothing to define. Noticed only because `reopened` was being appended to that same vocabulary — nothing here re-reads a paragraph no check can see. | Low mechanically: no check reads prose and nothing downstream depends on it. Moderate for the file's authority — it is this repository's scope contract and the first place an outside reader is sent for status, and its opening paragraph describes a phase that has ended. The fix is one paragraph and touches the file `work-units` maps as `unit_index`, which [triage.md](triage.md) classes as work rather than wording. | Yes, 2026-08-04 | **Closed in M14, by putting the paragraph in the past tense rather than cutting it.** Cutting was the other option [m14.md](work/m14.md) allowed and it would have taken two live things with it: the reason a phase is scoped in this file rather than in a plan document, which governs phase three as much as it governed phase two, and the definitions of `planned` and `deferred`, which the `reopened` paragraph below it joins rather than repeats and which M14–M21 are currently using. What was false is gone — M8–M13 are stated as closed, and the claim that the table had used neither word is removed. **Not a reopening:** the paragraph belongs to the phase-two plan and no unit's *Done means* ever asserted it, which is the test [triage.md](triage.md) sets. |
